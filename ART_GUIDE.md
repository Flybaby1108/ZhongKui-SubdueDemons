# 钟馗伏魔记 — 美术资产规范

> 这份文档是给你（美术）的**唯一工作指南**。按此规范出图，我直接接入，不用返工。
> 有任何不清楚的地方，直接问我，规范可以一起调整。

---

## 1. 总体风格

- **风格定位**：复古街机像素风（参考 Tumble Pop / Snow Bros / 泡泡龙）
- **氛围**：明亮、可爱、彩色、轻松
- **目标观感**：让人一眼想到 90 年代街机厅

---

## 2. 分辨率规格（最重要，先定这个）

### 逻辑分辨率
- **游戏内部分辨率**：`320 × 240` 像素（4:3）
- **显示放大**：自动放大到 `1280 × 960`（4倍）或全屏
- **网页版**：自适应缩放，但内部仍是 320×240

### 像素单位（Tile Size）
- **基础格子**：`16 × 16` 像素
- 一个屏幕 = 20 × 15 格

### 角色与物体尺寸标准

| 对象 | 尺寸（像素） | 占格子数 | 备注 |
|------|------------|---------|------|
| 主角（站立） | 24 × 32 | 1.5 × 2 | 比格子稍大，更显眼 |
| 普通敌人（小） | 16 × 16 | 1 × 1 | 史莱姆类 |
| 普通敌人（中） | 24 × 24 | 1.5 × 1.5 | 哥布林类 |
| Boss | 48 × 48 或更大 | 3 × 3 | 后期再做 |
| 道具（食物/宝石） | 16 × 16 | 1 × 1 | 收集物 |
| 子弹 / 吸入气流 | 8 × 16 | 0.5 × 1 | 视觉效果 |
| 平台砖块 | 16 × 16 | 1 × 1 | 地形基础单位 |

> **关键**：所有素材必须按 1:1 像素绘制，**不要用矢量软件导出**，不要抗锯齿。

---

## 3. 调色板

### 推荐色数
- **单个角色**：4-8 色（不含透明）
- **整张关卡**：16-32 色

### 推荐调色板
建议使用经典限制调色板，统一风格：
- **PICO-8 调色板**（16 色）：最推荐，街机感强
- **DB16 调色板**（16 色）：DawnBringer 经典 16 色
- **Endesga 32**（32 色）：色彩更丰富，仍有复古感

> 你选好后告诉我，我会把这套色板放进项目，未来所有素材都基于它。

---

## 4. 动画规格

### 帧率
- **游戏运行帧率**：60 FPS
- **角色动画帧率**：8-12 FPS（每帧持续 5-8 个游戏帧）
- 这种"卡顿感"才是街机风的灵魂，**不要做太流畅**。

### 主角必需动画

| 动作 | 帧数 | 循环？ | 优先级 |
|------|------|-------|--------|
| 站立（Idle） | 2-4 帧 | 是 | 必需 |
| 行走（Walk） | 4-6 帧 | 是 | 必需 |
| 跳跃上升（Jump Up） | 1-2 帧 | 否 | 必需 |
| 下落（Fall） | 1-2 帧 | 否 | 必需 |
| 吸气（Vacuum）| 4 帧 | 是 | 必需 |
| 吐出（Shoot） | 2-3 帧 | 否 | 必需 |
| 受伤（Hurt） | 2 帧 | 否 | 必需 |
| 死亡（Die） | 4-6 帧 | 否 | 必需 |
| 胜利（Win） | 4 帧 | 是 | 可选 |

### 敌人必需动画（每种敌人）

| 动作 | 帧数 | 循环？ |
|------|------|-------|
| 移动（Move） | 2-4 帧 | 是 |
| 被吸入（Sucking） | 2-3 帧 | 否（变形过程）|
| 被吸成球状（Captured） | 1 帧 | 否（静态球） |
| 死亡爆炸（Die） | 3-4 帧 | 否 |

### 朝向规则
- **只画朝右的版本**，朝左由代码自动镜像翻转
- 这样可以省一半工作量

---

## 5. 文件格式与交付方式

### 文件格式
- **格式**：PNG，背景透明
- **位深**：8-bit 或 32-bit RGBA 都可以
- **不要用** JPG（会有压缩瑕疵）

### 交付方式（二选一，告诉我你偏好哪个）

**方式 A：Sprite Sheet（推荐）**
- 把同一角色的所有帧拼成一张大图
- 横向排列，每帧固定大小
- 文件命名：`player_walk.png`（4 帧 = 96×32 像素）

**方式 B：单帧文件**
- 每帧一个 PNG
- 文件命名：`player_walk_01.png`、`player_walk_02.png` ...
- 我会自动合成 sprite sheet

> 推荐用 **Aseprite** 软件作画，原生支持像素动画 + sprite sheet 导出，是行业标准。

---

## 6. 命名规范

所有美术文件统一命名格式：

```
{类别}_{对象}_{动作}_{帧号}.png
```

### 示例
- `char_player_idle_01.png`
- `char_player_walk_01.png` ~ `char_player_walk_04.png`
- `enemy_slime_move_01.png`
- `enemy_slime_captured.png`
- `prop_apple.png`
- `tile_grass_01.png`
- `bg_level1.png`
- `ui_heart.png`
- `ui_score_digit_0.png`

### 类别前缀
- `char_` 角色（主角、NPC）
- `enemy_` 敌人
- `boss_` Boss
- `prop_` 道具
- `tile_` 地形砖块
- `bg_` 背景
- `ui_` 界面元素
- `fx_` 特效（爆炸、烟雾）

---

## 7. 关卡背景与地形

### 背景层
- **远景背景**：320×240，整张图，不需要循环
- **中景**（可选）：可视差滚动的装饰元素

### 地形（Tile Set）
- 每张地形图都是 16×16
- 一套地形需要：
  - 平面顶部
  - 平面中间（实心）
  - 左边缘、右边缘
  - 角落（左上、右上、左下、右下）
  - 单独砖块
- 共约 8-12 张图组成一套完整 tileset

> 第一关用 1 套地形即可，先简单。

---

## 8. UI 元素

### 必需 UI（原型阶段）
- 心形图标（生命值，3 颗）
- 数字 0-9（计分用，每个 8×16）
- "GAME OVER" 字样
- "STAGE 1/2/3" 字样
- 暂停图标

> UI 字体：**用图片画字**，不用系统字体，街机感更强。

---

## 9. 第一阶段交付清单（原型）

为了让原型尽快跑起来，**第一批最少需要这些素材**：

### 必交（缺一不可）
- [ ] 主角：站立 2 帧 + 行走 4 帧 + 跳跃 1 帧 + 下落 1 帧 + 吸气 4 帧 + 吐出 2 帧
- [ ] 敌人 A（小型）：移动 2 帧 + 被吸入 1 帧（球状）+ 死亡 3 帧
- [ ] 敌人 B（中型）：移动 4 帧 + 被吸入 1 帧 + 死亡 3 帧
- [ ] 敌人 C（飞行）：飞行 2 帧 + 被吸入 1 帧 + 死亡 3 帧
- [ ] 1 套地形 tileset（约 10 张 16×16）
- [ ] 1 张关卡背景图（320×240）
- [ ] UI：心 + 数字 0-9
- [ ] 道具：苹果、元宝（地图内）；铜钱（击杀掉落）

### 可后做
- 主角受伤、死亡、胜利动画
- 第二、三关的地形和背景
- 音效（这个我会用免费素材库先垫上）

---

## 10. 工作流程建议

1. **先选调色板**（告诉我，我配置进项目）
2. **先画主角站立 + 行走**，发我占位用，我接入测试
3. **再画 1 种敌人**，让我跑通"主角吸敌人"这个核心交互
4. **核心交互跑通后**，按上面清单补齐

我用占位方块先把游戏搭起来，**你慢慢画**，画一个我替换一个，**互不阻塞**。

---

## 11. 工具推荐

| 工具 | 用途 | 价格 |
|------|------|------|
| **Aseprite** | 像素画 + 动画 | $20（强烈推荐） |
| Piskel | 网页版像素画 | 免费 |
| Photoshop | 你熟就用，但不专为像素 | 你已有 |
| LibreSprite | Aseprite 开源分支 | 免费 |

---

## 有疑问随时问我

任何规范不清楚、不合理、或者你有更好的想法，**直接告诉我**。这份文档不是法律，是工具，目的是让我们高效协作。
